## What

Describe the change.

## Why

What LiveOG use case does it improve? Link the issue if there is one.

## Verification

- [ ] `pnpm build && pnpm typecheck && pnpm test` pass locally
- [ ] I added or updated tests where relevant
- [ ] I added a changeset (`pnpm changeset`) if a published package changed
- [ ] I did not claim unsupported platform behaviour
- [ ] Static fallback behaviour remains intact
